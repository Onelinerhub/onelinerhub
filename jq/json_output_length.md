# Get the array’s length property

```bash
somecommand | jq 'length'
```

- somecommand - any command that has to output json, `echo '[1,2,3]'` for example
- 'length' - function, which returns the array’s length property
