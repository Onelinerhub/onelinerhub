# How to install Xvfb on Ubuntu UBUNTU_VERSION

```bash
apt install xvfb
```

- `apt install` - installs Ubuntu package
- `xvfb` - virtual framebuffer app to launch GUI apps in background

group: install


