# How to reindex all plain indexes

```bash
indexer --all --rotate
```

- `indexer` - tool to manage indexes
- `--all` - will reindex all plain indexes
- `--rotate` - will ask search server to refresh data after indexing is complete

group: indexer


