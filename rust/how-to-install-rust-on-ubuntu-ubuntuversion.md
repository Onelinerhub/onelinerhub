# How to install rust on Ubuntu UBUNTU_VERSION

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

- `https://sh.rustup.rs` - installation script URL
- `curl` - will download installation script
- `| sh` - piping to shell to execute installation script when downloaded


