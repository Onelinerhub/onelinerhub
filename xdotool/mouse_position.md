# Get mouse position from CLI

```bash
xdotool getmouselocation --shell
```

- xdotool - tool to manipulate mouse/keyboard from CLI (```apt install xdotool```)
- getmouselocation - will return X/Y coordinates of mouse cursor

group: mouse
