# How to simulate a keystroke from CLI

```bash
xdotool key a
```

- xdotool - tool to manipulate mouse/keyboard from CLI (```apt install xdotool```)
- key a - ```a``` key press will be simulated

group: keys
