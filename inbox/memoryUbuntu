# Short solution needed: "Get memory usage in CLI" (ubuntu) #943

1. using top
top         #gives information about resource being utilized by each process.


2. using free

free
free -m option gives you memory utilization in MBs.
free -g option gives you memory utilization in GBs.

