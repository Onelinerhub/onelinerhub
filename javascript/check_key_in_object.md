# Check if certain key exists in object

```javascript
var exists = 'some_key' in obj
```

- exists - will contain ```true``` if ```some_key``` exists in ```obj```
- some_key - name of the key to check existence of
- obj - object to check existence in
