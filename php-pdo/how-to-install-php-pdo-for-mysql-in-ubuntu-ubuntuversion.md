# How to install PHP PDO for Mysql in Ubuntu UBUNTU_VERSION

```php-pdo
apt install php-mysql
```

- `apt install` - installs specified package on Ubuntu
- `php-mysql` - will install PDO along with Mysql drivers for PHP


