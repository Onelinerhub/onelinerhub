# Select database with PHP PDO

```php
$pdo = new PDO('mysql:host=localhost;dbname=test', 'user', 'pwd');
```

- `$pdo = new PDO` - connects to Mysql
- `dbname=` - specifies name of the database to choose

group: connect


