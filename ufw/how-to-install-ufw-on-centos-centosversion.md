# How to install ufw on Centos CENTOS_VERSION

```ufw
sudo -s
dnf install epel-release -y
dnf install ufw -y
```

- `epel-release` - installs EPEL repository on the server
- `sudo -s` - become root
- `install ufw` - installs ufw

group: install

