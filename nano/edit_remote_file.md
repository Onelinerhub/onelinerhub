# How to edit a remote file

```bash
ssh -t login@server.com 'nano /path/to/file.txt'
```

- login@server.com - remote server address and user to login under
- /path/to/file.txt - path to a file on a remote server
