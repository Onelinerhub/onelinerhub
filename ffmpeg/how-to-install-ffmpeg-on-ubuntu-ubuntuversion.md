# How to install ffmpeg on Ubuntu UBUNTU_VERSION

```bash
apt install ffmpeg
```

- `apt install` - installs specified package on Ubuntu
- `ffmpeg` - name of the package to install

group: install


link_youtube: https://youtu.be/R_SB2BUy0f0
