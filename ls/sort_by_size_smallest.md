# Sort files by size, smallest files first

```bash
ls -lSr /tmp/*.png
```

- -lSr - will print detailed file info and sort list by size (smallest at the top)
- /tmp/\*.png - pattern to search

group: sort
