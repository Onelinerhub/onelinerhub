# Sort files by datetime, oldest at the top

```bash
ls -altr /tmp
```

- -altr - will print detailed file info and sort list by file modified time (oldest at the top)
- /tmp - base directory to list files in

group: sort
