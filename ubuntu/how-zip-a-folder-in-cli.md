# How zip a folder in CLI

```bash
zip -r test.zip /path/to/dir/
```

- `zip -r` - create ZIP archive from folder 
- `test.zip` - name of the archive that will be created
- `/path/to/dir/` - this folder will be zipped

group: zip


