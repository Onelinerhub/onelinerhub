# Disable browser autocomplete for inputs

```html
<input type="text" autocomplete="off" />
```

- `input` - standard input element
- `autocomplete="off"` - disables browser default autocomplete


link_youtube: https://youtu.be/a3YlNli5x7s
