# Include Lua script inside Lua script

```lua
require "some_file"
```

- `require` - will load specified package (Lua file)
- `some_file` - Lua will search for `header.lua` in search path and load it's code

group: include


