# How to create branch and switch to it

```bash
git checkout -b branch3
```

- `git` - base command
- `checkout -b` - create new branch and change to it
- `branch3` - name of the existing branch to switch to

group: branch


