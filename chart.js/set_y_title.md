# Set Y axis title (label)

```javascript
new Chart('chart', {
  // ...
  options: { scales: { y: { title: { text: 'Y Title', display: true } } } }
});
```

- 'chart' - id of ```canvas``` element
- 'Y Title' - sets this title for Y axis

group: titles
