# Get keys count in Redis

```bash
redis-cli DBSIZE
```

- `redis-cli` - launch Redis CLI interface in interactive mode
- `DBSIZE` - returns count of keys in currently selected DB

## Example: 
```bash
redis-cli DBSIZE
```
```
(integer) 4
```

