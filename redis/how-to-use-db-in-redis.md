# How to use DB in Redis

```bash
redis-cli SELECT test
```

- `redis-cli` - executes Redis command in bash
- `SELECT` - sets specified DB as current (creates if it doesn't exist)
- `test` - name of the DB to select


